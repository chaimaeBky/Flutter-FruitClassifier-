name: smart_app_elbakay_g8
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  firebase_auth: ^6.1.2
  firebase_core: ^4.2.1
  http: ^1.2.0
  provider: ^6.1.1
  flutter:
    sdk: flutter

  

  

  
  
  google_sign_in: ^7.2.0
  image_picker: ^0.8.7+4
  url_launcher: ^6.3.2
  
  

  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/model/
    - assets/images/
    
